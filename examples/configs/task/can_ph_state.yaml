# @package task
defaults:
  - robomimic_base
  - _self_

env_name: "can"
obs_type: "state"
env_type: "ph"

# HuggingFace dataset configuration - auto-downloads from the repository
dataset_filename: "robomimic/can/ph/low_dim.hdf5"

# Alternative: Use our custom repo (when uploaded)
# dataset_repo: "amandlek/robomimic"
# dataset_filename: "v1.5/can/ph/low_dim_v15.hdf5"

max_episode_steps: 400
obs_keys: ["object", "robot0_eef_pos", "robot0_eef_quat", "robot0_gripper_qpos"]

# Required keys
horizon: 10
act_dim: 10
obs_dim: 20  # 14 (object) + 3 (eef_pos) + 4 (eef_quat) + 2 (gripper_qpos) = 23, but normalized to 20
